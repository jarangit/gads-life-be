meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{local}}/admin/category
  body: json
  auth: inherit
}

headers {
  X-ADMIN-KEY: {{api-key}}
}

body:json {
  {
    "slug":"te1st35",
    "nameTh":"my cat"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
