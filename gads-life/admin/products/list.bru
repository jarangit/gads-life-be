meta {
  name: list
  type: http
  seq: 4
}

get {
  url: {{local}}/admin/products?status=darft
  body: none
  auth: inherit
}

params:query {
  status: darft
}

headers {
  X-ADMIN-KEY: {{api-key}}
}

settings {
  encodeUrl: true
  timeout: 0
}
