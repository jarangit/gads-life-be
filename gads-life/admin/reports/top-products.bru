meta {
  name: top-products
  type: http
  seq: 2
}

get {
  url: {{local}}/admin/reports/top-products
  body: none
  auth: inherit
}

params:query {
  ~from: {{reportFrom}}
  ~to: {{reportTo}}
  ~limit: {{reportLimit}}
}

headers {
  X-ADMIN-KEY: {{api-key}}
}

settings {
  encodeUrl: true
  timeout: 0
}
