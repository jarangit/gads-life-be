meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{local}}/admin/collections
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  X-ADMIN-KEY: {{api-key}}
}

body:json {
  {
    "type": "TOP_LIST",
    "slug": "shop-special-price",
    "titleTh": "เลือกซื้อในราคาพิเศษ"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
