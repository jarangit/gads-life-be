meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{local}}/admin/brands
  body: json
  auth: inherit
}

headers {
  X-ADMIN-KEY: {{api-key}}
}

body:json {
  {
    "slug":"apple",
    "name":"Apple"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
