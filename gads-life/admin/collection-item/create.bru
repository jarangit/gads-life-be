meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{local}}/admin/collection-item
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  X-ADMIN-KEY: {{api-key}}
}

body:json {
  {
  }
}

settings {
  encodeUrl: true
}
