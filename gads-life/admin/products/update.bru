meta {
  name: update
  type: http
  seq: 5
}

patch {
  url: {{local}}/admin/products/XRF4MP3mIf
  body: json
  auth: inherit
}

headers {
  X-ADMIN-KEY: {{api-key}}
}

body:json {
  {
      "status": "published"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
